"""Sector definitions of the Godley-Lavoie GROWTH model (Chapter 11).

Each module registers its sector's variables, parameters and equations onto a
pysolve model via ``add_<sector>_variables/params/equations``.
"""
